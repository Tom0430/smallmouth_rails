<h2>チャレンジ失敗...</h2>
<% @goals.each do |goal| %>
    <%= link_to user_goal_path(goal.user.id, goal.id) do %>
        <%= goal.title %>
        <%= goal.detail %>
        <%= goal.created_at.strftime("%Y-%m-%d %H:%M:%S") %>
    <% end %>
    <hr>
<% end %>

