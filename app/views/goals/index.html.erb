<h2>goals</h2>

<% @goals.each do |goal| %>
    <%= link_to goal_path(goal.id) do %>
        <%= goal.title %>
        <%= goal.detail %>
        <%= goal.created_at.strftime("%Y-%m-%d %H:%M:%S") %>
    <% end %>
    <hr>
<% end %>



<%= link_to"新しい目標を立てる", new_goal_path %>